
class MACleanersApp{constructor(){this.init()}init(){this.bindIncludes();this.bindScrollTop();this.bindAnimations()}bindIncludes(){document.addEventListener('include:loaded',()=>{this.bindScrollTop();this.bindAnimations()})}bindScrollTop(){const btn=document.querySelector('.to-top');if(!btn||btn.dataset.ready)return;btn.dataset.ready='true';const toggle=()=>btn.classList.toggle('is-visible',window.scrollY>500);window.addEventListener('scroll',toggle,{passive:true});toggle();btn.addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}))}bindAnimations(){const items=[...document.querySelectorAll('[data-animate]:not([data-observed])')];if(!items.length)return;if(!('IntersectionObserver'in window)){items.forEach(i=>i.classList.add('is-visible'));return}const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(!entry.isIntersecting)return;entry.target.classList.add('is-visible');observer.unobserve(entry.target)})},{threshold:.18});items.forEach(item=>{item.dataset.observed='true';observer.observe(item)})}}
document.addEventListener('DOMContentLoaded',()=>new MACleanersApp());
